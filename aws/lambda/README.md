# AWS Lambda

A simple Go function deployed with [serverless](https://www.serverless.com/) and CORS enabled on AWS Lambda and the API Gateway

Use `make deploy` to build and deploy through AWS S3

You can destroy all the resources created with the command `serverless remove`
